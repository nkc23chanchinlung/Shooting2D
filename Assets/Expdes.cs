using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Expdes : MonoBehaviour
{
    // Start is called before the first frame update
    public void ExpDel()
    {
        Destroy(gameObject);
    }

   
}
